def make_negative( number ):
    if number > 0:
        number = "-"+ str(number)    
    return int(number)
    
    make_negative( 5 )
    
    
    make_negative( 0 )
    
    
    make_negative( -1 )
